package com.maps.resources;

import com.maps.db.PoiDAO;
import jakarta.ws.rs.GET;
import jakarta.ws.rs.Path;
import jakarta.ws.rs.Produces;
import jakarta.ws.rs.core.MediaType;
import jakarta.ws.rs.core.Response;

import java.nio.charset.StandardCharsets;

@Path("/api/pois")
@Produces(MediaType.APPLICATION_JSON)
public class PoiResource {

    private final PoiDAO poiDAO;

    public PoiResource(PoiDAO poiDAO) {
        this.poiDAO = poiDAO;
    }

    @GET
    public Response getPois() {
        try {
            // ⚡ Bolt: Query PostGIS directly for the full GeoJSON string using json_build_object and json_agg.
            // This avoids creating temporary Java objects and avoids Jackson overhead.
            String geoJson = poiDAO.getGeoJson();

            // ⚡ Bolt: Return the string as a byte array to bypass Jackson double-encoding serialization
            // since this resource is annotated with @Produces(MediaType.APPLICATION_JSON).
            return Response.ok(geoJson.getBytes(StandardCharsets.UTF_8)).build();
        } catch (Exception e) {
            return Response.serverError().entity(e.getMessage()).build();
        }
    }
}
